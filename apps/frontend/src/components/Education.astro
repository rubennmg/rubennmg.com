---
import EducationItem from "./EducationItem.astro";
const { items, currentLabel } = Astro.props;
---

<div class="grid gap-4 lg:grid-cols-3">
    {
        items.map((item) => (
            <EducationItem {...item} currentLabel={currentLabel} />
        ))
    }
</div>
