# Infrastructure

Infrastructure files for local and VPS staging environments.

- `staging/compose.yml`: Docker Compose stack for Caddy, Astro frontend, FastAPI backend and PostgreSQL.
- `staging/Caddyfile`: reverse proxy configuration for `rubennmg.cloud` and `api.rubennmg.cloud`.
